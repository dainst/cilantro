<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE issues PUBLIC "-//PKP//OJS Articles and Issues XML//EN" "http://pkp.sfu.ca/ojs/dtds/2.4.8/native.dtd">
<issues>
    <issue identification="{{data['identification']}}" published="{{data['auto_publish_issue']}}">

        {% if data['volume'] %}
        <volume>{{data['volume']}}</volume>
        {% endif -%}

        {% if data['number'] %}
        <number>{{data['number']}}</number>
        {% endif -%}

        {% if data['year'] %}
        <year>{{data['year']}}</year>
        {% endif -%}

        {% if data['description'] %}
        <description>{{data['description']}}</description>
        {% endif %}

        <section>
            <title locale="en_US">Articles</title>

            {%- for article in articles %}

                <article {{article.locale}}>
                    <title>{{article.title}}</title>
                    <id type="other::zenon">{{article.zenonId}}</id>
                    {% for author in article.author %}
                    <author>
                        <firstname>{{author.firstname}}</firstname>
                        <lastname>{{author.lastname}}</lastname>
                        <email>no@email.given</email>
                    </author>
                    {% endfor %}
                    <pages>{{article.pages_new}}</pages>
                    <date_published>{{article.date_published}}</date_published>
                    {# Might be useful later  <auto_publish_article>{{article.auto_publish}}</auto_publish_article>#}
                    {% if article.filepath %}
                    <galley>
                        <label>PDF</label>
                        <file>
                            <href src="{{article.filepath}}" mime_type="application/pdf" />
                        </file>
                    </galley>
                    {% endif %}
                </article>

            {% endfor %}
        </section>

    </issue>
</issues>
