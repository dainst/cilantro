<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE issues PUBLIC "-//PKP//OJS Articles and Issues XML//EN" "http://pkp.sfu.ca/ojs/dtds/2.4.8/native.dtd">
<issues>
    <issue identification="{{metadata['identification']}}" published="{{ojs_metadata['auto_publish_issue']|lower}}">

        {% if metadata['description'] %}
        <description>{{metadata['description']}}</description>
        {% endif %}

        {% if metadata['volume'] %}
        <volume>{{metadata['volume']}}</volume>
        {% endif -%}

        {% if metadata['number'] %}
        <number>{{metadata['number']}}</number>
        {% endif -%}

        {% if metadata['year'] %}
        <year>{{metadata['year']}}</year>
        {% endif -%}

        <section>
            <title locale="en_US">Articles</title>

            {% for article in articles %}
                <article {{'language="' + article['language'][0:2] + '" locale="' + article['language'] + '"'}}>
                    <title>{{article.title}}</title>
                    {% if article.zenonId and article.zenonId != '(((new)))'%}
                    <id type="other::zenon">{{article.zenonId}}</id>
                    {% endif %}
                    {% for author in article.author %}
                    <author>
                        <firstname>{{author.firstname}}</firstname>
                        <lastname>{{author.lastname}}</lastname>
                        <email>no@email.given</email>
                    </author>
                    {% endfor %}
                    <pages>{{article['pages']['showndesc'] + ('#DFM' if article['createFrontpage'] else '')}}</pages>
                    <date_published>{{article.date_published}}</date_published>
                    {% if article.filepath %}
                    <galley>
                        <label>PDF</label>
                        <file>
                            <href src="{{work_path}}/{{article.filepath}}" mime_type="application/pdf" />
                        </file>
                    </galley>
                    {% endif %}
                </article>

            {% endfor %}
        </section>

    </issue>
</issues>
